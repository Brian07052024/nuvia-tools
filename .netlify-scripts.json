{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "netlify:deploy": "npm run build && netlify deploy --prod",
    "netlify:draft": "npm run build && netlify deploy"
  }
}
